<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.brayandavid.homemedicines">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".Registro" />
        <activity android:name=".LoginUser"></activity>
        <activity android:name=".ProfileActivity" />
        <activity
            android:name=".View.ProductsActivity"
            android:label="@string/title_activity_productos"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".View.ServicesListActivity"
            android:label="@string/title_servicios_list"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.ServicesDetailActivity"
            android:label="@string/title_servicios_detail"
            android:parentActivityName=".View.ServicesListActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.brayandavid.homemedicines.View.ServicesListActivity" />
        </activity>
    </application>
</manifest>